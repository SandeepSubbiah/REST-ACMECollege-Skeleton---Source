1, Install Java 17 and Maven 3.
2, With IDE, make sure you have all the dependencies in the POM file is downloaded and ready to use
3, If not go to the project folder, do a "mvn clean install" then "mvn spring-boot:run".
4, Run the testcases in the IDE to run the all the testcases.